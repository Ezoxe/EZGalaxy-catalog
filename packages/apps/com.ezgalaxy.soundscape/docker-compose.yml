services:
  app:
    build: .
    container_name: ezgalaxy-com.ezgalaxy.soundscape
    ports:
      - "10019:3000"
    volumes:
      - app-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
    restart: unless-stopped

volumes:
  app-data: