services:
  app:
    build: .
    container_name: ezgalaxy-com.ezgalaxy.budget
    ports:
      - "10001:3000"
    volumes:
      - app-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
    restart: unless-stopped

volumes:
  app-data: